#!/bin/bash

# starts audio system
runuser -l predic -c "/home/predic/pre.di.c/.venv/bin/python /home/predic/pre.di.c/startaudio.py"

# loads irexec remote command launcher
#su -l predic -c "/usr/bin/irexec -d"
runuser -l predic -c "/usr/bin/irexec -d"
exit 0
